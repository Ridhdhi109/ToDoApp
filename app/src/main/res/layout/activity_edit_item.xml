<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.ridhdhi.todoapp.EditItemActivity">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btSave"
        android:onClick="onSaveItem"
        android:id="@+id/btSave"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textMultiLine"
        android:ems="10"
        android:id="@+id/etItemToEdit"
        android:layout_above="@+id/btSave"
        android:layout_alignRight="@+id/btSave"
        android:layout_alignEnd="@+id/btSave" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/editItem"
        android:id="@+id/tvEditItem"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/etItemToEdit"
        android:layout_alignStart="@+id/etItemToEdit"
        android:layout_alignRight="@+id/btSave"
        android:layout_alignEnd="@+id/btSave" />
</RelativeLayout>
